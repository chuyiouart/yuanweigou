# 视觉艺术早报逐条配图合同

生效日期：2026-08-13（北京时间）

## 目标

从生效日起，网站版视觉艺术早报的第一至第四新闻栏目中，**每一个编号新闻条目必须对应一张图片**。任何条目缺图、图片来源或授权信息不完整时，网站发布必须 `fail closed`；不得退回纯文字页面。

## 图片来源

1. 优先使用新闻涉及机构、博物馆、画廊、艺术家或开放馆藏提供的可复用图片。
2. 必须保存对象页或图片说明页，而不是只保存裸图片地址。
3. 必须保存公开授权/公版说明链接。
4. 如果图片只是用于解释新闻主题、并非事件现场照片，`thematic` 必须为 `true`；网站会明确显示“主题配图，非事件现场”。
5. 不得使用来源和权利状态不明的搜索缩略图，不得把 Google News RSS 缩略图直接当成授权来源。

## Package schema 2 扩展

`art-briefing` entry 从 2026-08-13 起必须满足：

- `image_files`：与第一至第四栏目中的编号新闻条目数量相同；顺序与正文标题顺序完全一致。
- `image_sha256`：每张源图一个 SHA-256，顺序一致。
- `story_images`：每条新闻一个对象，顺序一致，结构如下：

```json
{
  "heading": "正文中对应的新闻标题（不含序号）",
  "alt": "准确描述图片内容的替代文本",
  "credit": "图片机构或作者署名",
  "source_url": "https://图片对象页或机构说明页",
  "rights_url": "https://公开授权或公版说明页",
  "thematic": true
}
```

`story_images.heading` 必须逐字等于正文中对应的编号标题。字段缺失、额外字段、顺序漂移、非 HTTP(S) 链接或 `thematic != true` 均拒绝发布。

## 网站派生图

发布器只读源图并生成网站专用派生图：

- WebP；
- 最长边不超过 480px；
- 单张不超过 80KB；
- 内容哈希文件名；
- 明确宽高；
- `loading="lazy"`、`decoding="async"`；
- 源图和其 SHA 保留，不覆盖、不回写。

桌面端图片作为条目内小型解释图，移动端缩为约 112px 宽的图文行，不使用大图首屏或整页画廊。

## 范围与隔离

- 只适用于视觉艺术**早报**，不纳入晚报。
- 不修改 METRION 每日新构的四图合图合同。
- 不改变 Telegram 早报交付；网站仍是独立下游。
- 2026-08-12 及以前的历史页面保持兼容，不强制回填。
