# 元维构官网咨询助手说明

更新时间：2026-06-04

当前扩展阶段：版权授权 Agent v1。作品判断说明见 `METRION_ARTWORK_AGENT.md`，教育机构合作说明见 `METRION_EDUCATION_AGENT.md`，价格报价说明见 `METRION_PRICING_AGENT.md`，版权授权说明见 `METRION_RIGHTS_AGENT.md`。

## 一句话定位

元维构官网咨询助手是一个面向外部访问者的项目问答入口，用于解释元维构 METRION 的艺术结构转译服务、作品提交、教育机构合作、价格区间、版权边界、样本数据、数字档案和展会扫码咨询。

它不是内部销售话术库，也不是给项目方自己练习介绍项目的工具。所有显示给用户的内容都必须站在访问者视角，直接回答“我想了解什么”“我能不能做”“我需要准备什么”“我可以怎么合作”。

## 当前使用位置

- 独立页面：`agent.html`
- 主要逻辑：`agent.js`
- 全站悬浮入口：`pwa.js`
- 样式：`styles.css`
- 缓存与版本控制：`sw.js`

全站悬浮入口会在加载 `pwa.js` 的页面左下角显示“问问元维构”。点击后展开问答面板，关闭按钮可以收起。`agent.html` 本身不重复显示悬浮面板。

## 用户口吻原则

必须使用外部用户能自然理解的表达。

推荐表达：

- “元维构是什么？”
- “我的作品适合做成立体作品吗？”
- “教育机构 30 个学生可以怎么合作？”
- “展会现场扫码后可以了解什么？”
- “提交作品前需要准备哪些资料？”
- “价格大概怎么判断？”
- “版权和授权销售怎么处理？”

避免出现在用户界面中的内部表达：

- “可直接使用的话术”
- “咨询话术”
- “怎么回复客户”
- “用户问我项目是什么，我应该怎么回复”
- “展会现场怎么介绍元维构”
- “判断清单”
- “销售话术”
- “套路”

如果未来确实需要给项目方准备内部沟通话术，应该单独建立内部工作台或内部文档，不要混在官网用户端 agent 中。

## 回答边界

可以直接回答：

- 项目定义和服务范围
- 元维构与普通 3D 打印店的区别
- 儿童画、青少年作品、插画、平面作品是否可能转译
- 教育机构批量合作的基本流程
- 样本数据表的作用
- 数字档案、二维码档案、AR/MR、虚拟展厅的用途
- 展会扫码后用户可以了解什么
- 常见价格判断逻辑和影响因素

必须提醒人工确认：

- 具体作品是否适合转译
- 是否可以公开展示
- 是否可以授权销售
- 是否拥有源文件或生产级模型
- 最终报价
- 合同条款
- 版权归属、监护人授权、机构宣传授权

不要承诺：

- “一定可以做”
- “一定可以销售”
- “版权完全没问题”
- “价格固定”
- “自动完成最终判断”

## 知识模块

`agent.js` 中的 `AGENT_KNOWLEDGE` 是核心知识库。每个模块一般包含：

- `id`：模块标识
- `title`：回答标题
- `category`：显示分类
- `keywords`：匹配关键词
- `summary`：主回答
- `bullets`：补充说明
- `templates`：不同场景下的正文补充。注意：前端不应显示“话术”标题。
- `collect`：需要用户补充的信息。用户端显示为“为了继续判断，可以补充这些信息”。
- `links`：相关页面入口
- `followups`：后续推荐问题
- `caution`：谨慎说明

当前重点模块包括：

- 项目定义
- 与普通 3D 打印或建模服务的区别
- 教育机构与儿童画批量合作
- 家长与儿童作品纪念
- 作品适配判断
- 价格与报价
- 版权、授权和公开展示边界
- 样本数据表
- 展会扫码与现场咨询
- 数字档案、AR/MR 与虚拟展厅
- 空间合作
- 人工沟通与高敏问题转接

## 悬浮助手交互规则

默认状态：

- 页面左下角只显示“问问元维构”按钮。
- 不默认打开大对话框。

打开后：

- 首次打开显示欢迎回答。
- 点“项目介绍 / 作品判断 / 教育合作”等按钮会自动提问。
- 输入框点“发送”会加载 `agent.js` 并回答。
- 用户提问使用浅暖色气泡，系统回答使用白色气泡。
- 提问后滚动位置停留在“用户问题 + 回答开头”，不要自动跳到回答底部。
- 点叉号关闭面板。

## 缓存与版本号

因为网站启用了 service worker，修改 `agent.js`、`pwa.js`、`styles.css`、`sw.js` 后要同步更新版本号，否则线上可能继续加载旧文件。

当前版本号：

```text
20260606-rights-agent-v1
```

涉及位置：

- HTML 中的 `styles.css?v=...`
- HTML 中的 `pwa.js?v=...`
- `agent.html` 中的 `agent.js?v=...`
- `pwa.js` 中动态加载的 `agent.js?v=...`
- `pwa.js` 中的 `METRION_BUILD_VERSION`
- `sw.js` 中的 `METRION_CACHE`

每次改 agent 后，建议把版本号改成新的语义化短标识，例如：

```text
20260604-agent-pricing-fix
20260604-agent-education-copy
20260604-agent-rights-update
```

## 发布注意事项

当前发布仓库：

```text
C:\Users\saint\Desktop\其他\github-publish-yuanweigou-v57-20260530
```

工作站点：

```text
C:\Users\saint\Desktop\其他\yuanweigou-site
```

同步发布时，不要误提交无关文件。目前 `quest-gallery.html` 可能仍有未提交的旧改动，如果不是专门更新 VR 美术馆页面，不要把它和 agent 修复一起提交。

Codex 当前环境经常无法直接访问 GitHub，所以通常流程是：

1. Codex 修改本地站点。
2. Codex 同步到发布仓库并 commit。
3. 用户在 GitHub Desktop 点击 `Push origin`。

## 测试链接

普通首页：

```text
https://chuyiouart.github.io/yuanweigou/index.html#top
```

带版本测试链接：

```text
https://chuyiouart.github.io/yuanweigou/index.html?v=20260604-public-questions#top
```

独立咨询助手：

```text
https://chuyiouart.github.io/yuanweigou/agent.html
```

## 后续扩展方向

优先扩展：

- 作品适配判断标准更细化
- 教育机构批量合作流程更完整
- 价格区间与影响因素更清晰
- 授权销售、公开展示、儿童作品监护人授权边界更明确
- 样本数据表与具体案例联动
- 表单提交后进入人工判断流程

谨慎扩展：

- 自动报价
- 自动判断版权状态
- 自动承诺可销售
- 自动替代人工审核
- 投资人或内部商业模式说明直接暴露给官网访客

这些内容可以做内部版本，但不应直接放入官网用户端 agent。
